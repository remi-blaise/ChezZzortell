tags:           [ font, css ]
creationDate:   ~
modifDate:      ~

! Ne pas mettre `sans-serif` entre guillemets
===

Utiliser `font-family: "sans-serif"` est mal interprété.

Il faut donc mettre directement :
```css
font-family: Verdana, sans-serif;
```
